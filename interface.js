// function showTodo(todo: { title: string; text: string }) {
//   console.log(todo.title + " " + todo.text);
// }
function showTodo(todo) {
    console.log(todo.title + " " + todo.text);
}
var hiTodo = { title: "Hello", text: "How you do" };
showTodo(hiTodo);
